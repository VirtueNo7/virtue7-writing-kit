# Form Lock Gate

Apply the selected output profile before the lock. In Narrative Lock, block body bullets, numbered-list drift, fragment stacks, excessive interior headings, and visible outline scaffolding unless explicitly requested. In Preserve Form, compare against approved form evidence. In Adaptive mode, enforce the selected profile. A legitimate structured artifact may require lists, fields, tables, or steps.
